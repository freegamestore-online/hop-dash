{
  "name": "hop-dash",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @hop-dash/web dev",
    "build": "pnpm --filter @hop-dash/web build",
    "preview": "pnpm --filter @hop-dash/web preview",
    "typecheck": "pnpm --filter @hop-dash/web exec tsc -b",
    "test": "pnpm --filter @hop-dash/web test"
  }
}
